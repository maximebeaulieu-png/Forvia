Every extracted value carries one. It is the provenance affordance: page + verbatim quote on hover.

```jsx
<ConfidenceDot value={0.82} page={2} quote="Frais de retrait engagés par l'assuré 305.000 €" />
<ConfidenceDot value={0.93} showValue />   // accuracy column
```

Shape carries the level, not colour — the dot stays ink so it never competes with status.
