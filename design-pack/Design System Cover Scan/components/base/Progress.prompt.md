Generic track — compliance share, processing total, chart bars. For required-vs-found use `GapBar`, never `Progress`.

```jsx
<Progress value={78} label="78 %" />
<Progress value={9} max={150} tone="go" label="9 / 150" />
```
